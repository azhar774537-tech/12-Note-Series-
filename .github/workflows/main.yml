name: Auto Build React Native App to APK

# Define your event triggers here
on:
  push:
    branches:
      - main     # Change this to your default branch name if it is 'master' or something else
  pull_request:
    branches:
      - main
  workflow_dispatch: # Allows you to also run this manually from the GitHub website

jobs:
  # Your build jobs go here...
